# Exports

Condensed or adapted versions of this repo's rule-set intended for use in
external agents or harnesses. Files here are NOT loaded by the Claude Code
harness that reads `CLAUDE.md` / `rules/`.

When upstream sources under `rules/` change, update the corresponding
export file in this directory in the same change-set to keep the exported
version in sync.
